-- Migration: Add daily_basic columns to ml_training_dataset
-- Date: 2026-01-03

ALTER TABLE ml_training_dataset
    ADD COLUMN IF NOT EXISTS turnover_rate DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS turnover_rate_f DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS pe DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS pe_ttm DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS pb DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS ps DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS ps_ttm DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS dv_ratio DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS dv_ttm DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS total_share DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS float_share DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS free_share DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS total_mv DOUBLE PRECISION,
    ADD COLUMN IF NOT EXISTS circ_mv DOUBLE PRECISION;
